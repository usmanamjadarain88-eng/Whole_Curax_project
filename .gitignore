# =============================================================================
# Work/ monorepo — private, machine-local, and generated files (all projects)
# =============================================================================

# Standalone Git repo + Vercel app (do not track here — use c:\Work\3d-model\.git)
3d-model/

# --- Nested .git folders (single repo at Work/ root only) ---
**/.git/
!/.git/

# --- Local backup of old per-folder .git (never push — huge + wrong history) ---
**/.git.bak/

# --- Secrets & environment (never commit) ---
.env
.env.*
!.env.example
.env.local
.env.*.local
*.pem
*.p12
**/credentials.json
**/service-account*.json
**/google-services.json
**/GoogleService-Info.plist

# --- Personal / non-code files ---
*.docx
*.doc

# --- medicine-alerts: backend URLs / DB connection on disk ---
medicine-alerts/backend/.env
medicine-alerts/backend/database_url.txt
medicine-alerts/backend/backend_url.txt
medicine-alerts/backend/api_base_url.txt
medicine-alerts/backend/data_bus_url.txt

# --- medicine-alerts: desktop local DB (codes, hashes, user data) ---
medicine-alerts/frontend/curax_desktop.json

# --- databus: local secrets (template .env.example stays tracked) ---
databus/.env

# --- Python (all folders) ---
**/__pycache__/
**/*.py[cod]
**/$py.class
**/.Python
**/.venv/
**/venv/
**/env/
**/.eggs/
**/*.egg-info/
**/.pytest_cache/
**/.mypy_cache/
**/.ruff_cache/
**/dist/
**/*.egg

# --- Node / Expo / RN (medicine-alerts/mobile etc.) ---
**/node_modules/
**/.expo/
**/.turbo/
**/npm-debug.log*
**/yarn-error.log*

# --- Android / Gradle (curax_app/android) ---
# Facebook App ID / client token: use local.properties keys facebook.app.id, facebook.client.token (never commit)
curax_app/android/facebook.properties
curax_app/android/app/facebook.properties
**/facebook_secrets.xml
**/local.properties
**/.gradle/
**/build/
**/captures/
**/*.apk
**/*.aab
**/*.ap_
**/*.dex
**/*.class
**/*.keystore
**/*.jks
**/keystore.properties
**/signing.properties
hs_err_pid*.log
replay_pid*.log

# --- OS & editors ---
.DS_Store
Thumbs.db
Desktop.ini
.idea/
*.iml
.vscode/
*.swp
*.swo
*~

# --- Logs & temp ---
*.log
*.tmp
*.temp
.cache/

# --- Deploy tooling local state ---
.vercel/
.netlify/
