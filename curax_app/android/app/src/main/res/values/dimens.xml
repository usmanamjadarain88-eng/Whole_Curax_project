<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Auth headings: responsive top inset (was 102dp — caused clipping on short phones). -->
    <dimen name="auth_screen_title_margin_top">64dp</dimen>
    <!-- Decorative bottom WebView / asset strip on sign-up (keep modest so form stays visible). -->
    <dimen name="auth_bottom_decoration_height">120dp</dimen>
    <!-- Sign up register: subtitle first line from content top (design px → dp). -->
    <dimen name="signup_subtitle_margin_top">150dp</dimen>
    <!-- Sign-up mock: field row height -->
    <!-- Min inner height; vertical padding keeps hint/cursor inside outline on double-tap -->
    <dimen name="signup_field_height">42dp</dimen>
    <dimen name="signup_field_corner">5dp</dimen>
    <!-- Sign-up register: vertical anchors from content top (equal 60dp steps). -->
    <dimen name="signup_guide_names">208dp</dimen>
    <dimen name="signup_guide_email">268dp</dimen>
    <dimen name="signup_guide_password">328dp</dimen>
    <dimen name="signup_guide_confirm">388dp</dimen>
    <dimen name="signup_guide_or_social">458dp</dimen>
    <dimen name="signup_guide_social">508dp</dimen>
    <dimen name="signup_guide_marketing">592dp</dimen>
    <dimen name="signup_guide_terms">624dp</dimen>
    <dimen name="signup_guide_button">678dp</dimen>
    <dimen name="signup_footer_margin_top">760dp</dimen>
    <!-- Health hub insight strip: one title line + one subtitle line per card. -->
    <dimen name="health_hub_insight_card_width">158dp</dimen>
    <!-- Content column: stock snapshot card inner padding matches this gutter. -->
    <dimen name="standalone_dashboard_content_padding">12dp</dimen>
    <dimen name="standalone_sound_card_stroke">1dp</dimen>
    <dimen name="standalone_sound_card_stroke_selected">2dp</dimen>
</resources>
