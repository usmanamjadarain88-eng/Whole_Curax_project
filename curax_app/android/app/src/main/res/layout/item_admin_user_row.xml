<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardBackgroundColor="@color/summary_card"
    app:cardCornerRadius="12dp"
    app:cardElevation="1dp"
    app:strokeColor="@color/summary_stroke"
    app:strokeWidth="1dp">

    <LinearLayout
        android:id="@+id/rowAdminUserRoot"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="10dp"
        android:paddingTop="10dp"
        android:paddingEnd="8dp"
        android:paddingBottom="10dp">

        <FrameLayout
            android:layout_width="44dp"
            android:layout_height="44dp">

            <ImageView
                android:id="@+id/ivAdminUserAvatar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@null"
                android:scaleType="centerCrop"
                android:visibility="gone"
                tools:visibility="visible" />

            <TextView
                android:id="@+id/tvAdminUserInitial"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/bg_admin_hub_avatar"
                android:gravity="center"
                android:textColor="@color/button_primary_text"
                android:textSize="17sp"
                android:textStyle="bold"
                tools:text="S" />
        </FrameLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_weight="1"
            android:minWidth="96dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvAdminUserName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="2"
                android:textColor="@color/text_primary"
                android:textSize="15sp"
                android:textStyle="bold"
                tools:text="Sam Patient" />
        </LinearLayout>

        <TextView
            android:id="@+id/tvAdminUserMode"
            android:layout_width="88dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="2"
            android:textColor="@color/text_primary"
            android:textSize="12sp"
            android:textStyle="bold"
            tools:text="Standalone" />

        <TextView
            android:id="@+id/tvAdminUserStatus"
            android:layout_width="88dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="10dp"
            android:background="@drawable/bg_admin_hub_status_pill"
            android:gravity="center"
            android:paddingHorizontal="6dp"
            android:paddingVertical="6dp"
            android:textColor="@color/connection_panel_title"
            android:textSize="11sp"
            android:textStyle="bold"
            tools:text="Ready" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnAdminUserCareMode"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="96dp"
            android:layout_height="wrap_content"
            android:minWidth="0dp"
            android:minHeight="44dp"
            android:paddingHorizontal="6dp"
            android:text="@string/admin_users_care_mode"
            android:textAllCaps="false"
            android:textColor="#FFFFFFFF"
            android:textSize="12sp"
            app:backgroundTint="@android:color/black"
            app:cornerRadius="10dp"
            app:rippleColor="#44FFFFFF" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
